# 2025년 3월 5일 작업내역

## 주요 작업: UserSettings, UserSocialLink API 개발 및 UserSocialLink 기능 분리

### 1. **UserSettings API 개발**

#### **기능 구현**

사용자 설정 관리를 위한 RESTful API 컨트롤러를 개발함. 주요 설정 관리 기능은 다음과 같다:

- **개인 정보 설정**
  - 자기소개(bio) 업데이트
  - 사용자 테마 변경
  - 언어 설정 변경
- **알림 설정**
  - 이메일 마케팅 수신 동의 설정
  - 게시글 이메일 알림 설정
  - 댓글 이메일 알림 설정

#### **보안 특징**

- 모든 개인 설정 변경은 인증된 사용자 본인만 가능
- `@AuthenticationPrincipal`을 사용하여 사용자 인증 및 권한 검증
- 잘못된 접근 시 `SecurityException` 발생

#### **API 엔드포인트**

- `/api/v1/userSettings/{userId}/bio`
- `/api/v1/userSettings/{userId}/emailMarketing`
- `/api/v1/userSettings/{userId}/emailPostNotifications`
- `/api/v1/userSettings/{userId}/emailCommentNotifications`
- `/api/v1/userSettings/{userId}/theme`
- `/api/v1/userSettings/{userId}/language`

------

### 2. **UserSocialLink 기능 분리 및 API 개발**

#### **배경**

기존 `UserSettings`에서 소셜 링크를 관리하던 방식에서 발생하는 여러 문제점들을 해결하기 위해, `UserSocialLink`를 독립적인 엔티티로 분리하여 사용자 소셜 링크를 더 직관적으로 관리할 수 있도록 개선함.

#### **주요 개선 사항**

1. **엔티티 분리**
   - `UserSettings`에서 소셜 링크 관리 로직을 분리
   - `User` 엔티티와 직접적인 관계를 가지도록 구조 변경
2. **API 개발 (`UserSocialLinkController`)**
   - 소셜 링크에 대한 CRUD 기능 구현
   - RESTful API 엔드포인트 설계
     - **GET**: 소셜 링크 조회 (`/api/v1/socialLink/{userId}`)
     - **POST**: 소셜 링크 추가 (`/api/v1/socialLink/{userId}`)
     - **PUT**: 소셜 링크 수정 (`/api/v1/socialLink/{userId}`)
     - **DELETE**: 소셜 링크 삭제 (`/api/v1/socialLink/{userId}`)
3. **보안 강화**
   - `@AuthenticationPrincipal`을 활용한 사용자 인증
   - 자신의 프로필에만 소셜 링크 변경 가능하도록 제한
   - 인증되지 않은 사용자의 접근 차단
4. **DTO 활용**
   - `UserSocialLinkDTO` 생성
   - 민감한 정보 제외
   - API 응답의 안전성 및 효율성 개선
5. **경로 및 파라미터 설계**
   - `{userId}` 경로 변수를 통한 유연한 API 설계
   - `socialPlatform`을 쿼리 파라미터로 활용하여 세부 조작 가능

#### **해결된 문제점**

- 소셜 링크 관리의 모호성 해소
- 사용자별 소셜 링크를 독립적으로 관리 가능
- API 경로 및 파라미터 세부 설정
- 민감한 정보 노출 방지

#### **주의사항**

- Postman을 통한 초기 테스트 완료
- 실제 운영 환경에서의 최종 통합 테스트 필요

#### **다음 단계**

- 추가 기능 구현
- API 문서화
- 통합 테스트 진행
