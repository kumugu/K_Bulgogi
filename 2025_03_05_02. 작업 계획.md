# FE_작업 진행 순서서

### 1. 상태 관리 (Zustand)

Zustand를 사용하여 상태 관리를 설정함. 상태 관리는 인증 정보, 사용자 설정, 게시글 관련 정보 등을 저장하는 데 필요함.

#### 상태 관리 주요 항목

- **authStore**: 인증 관련 상태 (로그인, 로그아웃, JWT 토큰 등)
- **userSettingsStore**: 사용자 설정 관련 상태 (자기소개, 소셜 링크, 테마, 언어, 이메일 알림 등)
- **postStore**: 게시글 관련 상태 (최신 글 목록, 유저 게시글, 카테고리별 글, 좋아요/구독 기능 등)
- **profileStore**: 유저 프로필 관련 상태 (팔로우/팔로워, 본인 작성 게시글 등)

### 2. 프론트엔드 작업 진행 순서

1️⃣ **인증 관련 작업 (auth)**

- **기능 구현**: 로그인, 로그아웃 기능, JWT 토큰 저장 및 관리

- 구현 방법

  :

  - Zustand를 사용해 `authStore`에 JWT 토큰과 로그인 상태를 관리
  - 로그인 후 `localStorage`에 JWT 토큰 저장하고, 상태를 통해 로그인 여부 확인
  - 로그인된 사용자의 정보를 API에서 받아와 상태에 저장

2️⃣ **회원가입 기능 구현**

- **기능 구현**: 회원가입 폼 작성 및 API 연동

- 구현 방법

  :

  - 사용자 입력 폼 (이메일, 비밀번호, 이름 등)과 유효성 검사
  - 회원가입 후 자동으로 로그인되도록 처리하거나 로그인 페이지로 리디렉션

3️⃣ **유저 설정 페이지 (User Settings)**

- **기능 구현**: 사용자 설정 CRUD (자기소개 수정, 소셜 링크 추가/삭제, 이메일 알림 설정, 테마 변경, 언어 설정)

- 구현 방법

  :

  - Zustand를 사용해 `userSettingsStore` 상태 관리
  - 각 설정을 수정할 수 있는 UI 및 API 연동 (PATCH 요청)
  - 소셜 링크는 CRUD 기능을 제공하고, 나머지 설정은 PATCH로 수정

4️⃣ **마이페이지 및 블로그 기본 레이아웃 구축**

- **기능 구현**: 마이페이지에서 유저 프로필, 최신 게시글 목록 조회 및 API 연동

- 구현 방법

  :

  - 유저 프로필 및 게시글 목록을 보여주는 UI 설계
  - API에서 유저 정보를 받아와 상태에 저장하고, 렌더링
  - 특정 유저의 게시글 조회 기능

5️⃣ **게시글 CRUD 구현**

- **기능 구현**: 게시글 작성, 조회, 수정, 삭제 기능

- 구현 방법

  :

  - 마크다운 에디터를 사용하여 글 작성 기능 추가
  - 게시글 목록을 조회하고, 선택한 게시글을 수정/삭제할 수 있는 기능 구현
  - 좋아요, 구독 기능을 추가하여 유저 상호작용 구현

6️⃣ **유저 간 커뮤니케이션 기능**

- **기능 구현**: 팔로우/팔로워 기능, 댓글 시스템

- 구현 방법

  :

  - 팔로우/팔로워 목록을 보여주고 팔로우/언팔로우 기능을 추가
  - 댓글 시스템을 추가하여 게시글에 댓글을 작성하고 조회하는 기능 구현

### 3. 폴더 구조

프로젝트 구조를 어떻게 나눌지 정리함. 주요 폴더는 다음과 같다.

```csharp
src/
│
├── assets/                 # 이미지, 아이콘, 기타 정적 파일
├── components/             # UI 컴포넌트 (버튼, 입력창, 카드 등)
├── pages/                  # 각 페이지 구성 (로그인, 회원가입, 마이페이지 등)
│   ├── LoginPage.js
│   ├── RegisterPage.js
│   ├── UserSettingsPage.js
│   ├── ProfilePage.js
│   └── ...
├── stores/                 # Zustand 상태 관리 (authStore, userSettingsStore 등)
│   ├── authStore.js
│   ├── userSettingsStore.js
│   ├── postStore.js
│   └── profileStore.js
├── api/                    # API 통신 관련 파일
│   ├── authAPI.js
│   ├── userSettingsAPI.js
│   ├── postAPI.js
│   └── ...
├── utils/                  # 유틸리티 함수 (예: 날짜 포맷, 로컬 스토리지 관리 등)
├── styles/                 # CSS 스타일 파일
│   ├── global.css
│   ├── theme.css
│   └── ...
└── App.js                  # 루트 컴포넌트
```

### 4. 각 기능 구현 방법

- **Zustand 상태 관리**: `create`를 사용하여 각 상태를 생성하고, 그 상태를 컴포넌트에서 사용함. 상태 변화가 필요할 때는 `set` 메서드를 통해 업데이트하고, `get`을 통해 현재 상태를 가져온다.
- **API 연동**: Axios나 Fetch를 사용하여 API 통신을 하고, 상태에 데이터를 저장함. API 응답을 처리할 때는 로딩 상태를 관리하는 것이 좋다.
- **컴포넌트 분리**: 각 기능을 컴포넌트로 분리하여 재사용성을 높이고, 각 페이지나 컴포넌트에서 필요한 상태를 Zustand 상태로 관리함.
