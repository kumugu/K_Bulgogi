# 2025년 3월 4일 작업 내역

## 1. **BE_UserSetting API 설계**

- 기존 FE에서 작업된 형식에 맞춰서 BE에서 API를 구현함.

- 관련 작업:

  - **Entity 설계**: `UserSettings` 엔티티 설계 및 구현.

  - **Repository 및 Service**: `UserSettings`를 관리하기 위한 Repository 및 Service 구현.

  - **Controller**: `UserSettings`에 대한 요청을 처리하는 Controller 구현.

  - 기능 추가

    :

    - 자기소개
    - 이메일 마케팅 설정
    - 게시글, 댓글 알림 설정
    - 테마 설정
    - 언어 설정

  - 인증 검증

    :

    - `@AuthenticationPrincipal CustomUserDetails customUserDetails`를 사용하여 인증된 사용자의 ID와 요청한 `userId`가 일치하는지 검증.

## 2. **BE_UserSocialLink 작업 진행**

- **문제 발견**: 기존에 `UserSettings`와 관계를 맺고 있던 `UserSocialLink`가, `userId`를 사용하여 연결되는 문제가 발생.

- 재설계 진행 중

  :

  - `UserSettings`와의 관계를 `User`로 변경하여 직관적으로 수정.
  - **DTO 사용**: `UserSettings`와 `UserSocialLink`에서 관계는 Entity 레벨에서 설정하고, 데이터를 가져올 때는 DTO를 사용하여 처리할 계획.

------

### 이후 해야 할 작업

## 1. **BE_UserSocialLink Entity 수정 및 최적화**

- `UserSettings`와의 관계를 `User`와 연결하는 방식으로 변경.
- 수정된 관계를 기반으로 Service와 Controller 수정.
- DTO 설계를 통해 데이터 전송을 최적화.

## 2. **FE 연동 작업**

- `UserSettings` 및 `UserSocialLink`와 관련된 데이터가 FE와 연동될 수 있도록 API 호출 및 응답 처리.
- FE에서 사용자 설정 및 소셜 링크 관련 UI 구현.

## 3. **테스트 및 배포 준비**

- 기능 테스트 및 디버깅.
- 배포를 위한 최종 확인 및 준비 작업.

