# 2025년 3월 4일 작업 계획

## 1. 백엔드 작업 (BE)
### 1.1. User 기능 구현
- 비밀번호 변경
- 이메일 변경
- 계정 삭제 기능 추가

### 1.2. UserSetting 기능 구현
- 자기소개
- 팔로우, 팔로잉 기능 추가
- 소셜 계정 등록
- 이메일 수신 설정
- 테마 변경 기능

## usersettings db 테이블에 기본 데이터를 주입시키는 방법에 관하여
1. 회원가입 시 자동으로 생성되게 할것인가
2. settings 페이지 진입 시 발생하게 할것인가
3. 설정내용을 변경하려고할때 값이 없으면 추가할 것인가
   에 대하여 고민,  1번 방법으로 진행함.
   다소 비효율적으로 불필요한 설정데이터가 추가될 수 있지만(이용하지 않는 이용자들의 데이터도 추가되기때문에)
   그러나 데이터의 내용이 비교적 적고, 값자체도 큰 값을 가지지 않기때문에 대규모 이용자들이 이용한다고 가정했을때 유의미한 결과가 발생하기때문에 큰 의미가 없다고 판단하여 1번 방식을 채택함.

## usersettings 내용중 socialLink 설정 내용은 따로 분리하여 crud 기능을 추가하기로 했고, 
settings 의 fk 인 user_id와 연결을 하여 가져오려고 했으나 문제가 있는거같아 직접 user와 관계를 가지고 데이터는 dto로 분리하여 가지고 오기로 함


## 2. 프론트엔드 상태
- 관련된 폼(비밀번호 변경, 이메일 변경, 계정 삭제, 프로필 수정 등) 생성 완료됨
- 백엔드 API 연동 필요

## 3. 백엔드 설계 및 개발 진행
### 3.1. Entity 설계
- `User` 엔티티 확장 필요
- `UserSettingInfo` 엔티티 추가

### 3.2. Service 및 Controller 구현
- User 관련 서비스 및 컨트롤러 구현
- UserSettingInfo 관련 서비스 및 컨트롤러 구현

### 3.3. Spring Security 설정
- `requestMatchers` 설정 필요 (비밀번호 변경, 이메일 변경, 계정 삭제 관련 보안 강화)

## 4. 개발 진행 순서
1. `UserSettingInfo` 및 `User` 엔티티 설계
2. Repository, Service, Controller 구현
3. Spring Security 설정 적용
4. 프론트엔드와 API 연동 및 테스트

